#!/bin/bash
# info: change web domain proxy template
# options: USER DOMAIN TEMPLATE [EXTENTIONS] [RESTART]
#
# The function changes proxy template


#----------------------------------------------------------#
#                    Variable&Function                     #
#----------------------------------------------------------#

# Argument defenition
user=$1
domain=$(idn -t --quiet -u "$2" )
domain_idn=$(idn -t --quiet -a "$domain")
template=$3
default_extentions="jpg,jpeg,gif,png,ico,svg,css,zip,tgz,gz,rar,bz2,doc,xls,\
exe,pdf,ppt,txt,odt,ods,odp,odf,tar,wav,bmp,rtf,js,mp3,avi,mpeg,flv,html,htm"
extentions=${4-$default_extentions}
restart="$5"


# Includes
source $VESTA/func/main.sh
source $VESTA/func/domain.sh
source $VESTA/func/ip.sh
source $VESTA/func/ipv6.sh
source $VESTA/conf/vesta.conf


#----------------------------------------------------------#
#                    Verifications                         #
#----------------------------------------------------------#

check_args '3' "$#" 'USER DOMAIN TEMPLATE [EXTENTIONS] [RESTART]'
validate_format 'user' 'domain' 'template'
is_system_enabled "$PROXY_SYSTEM" 'PROXY_SYSTEM'
is_object_valid 'user' 'USER' "$user"
is_object_unsuspended 'user' 'USER' "$user"
is_object_valid 'web' 'DOMAIN' "$domain"
is_object_unsuspended 'web' 'DOMAIN' "$domain"
is_object_value_exist 'web' 'DOMAIN' "$domain" '$PROXY'
is_proxy_template_valid $template


#----------------------------------------------------------#
#                       Action                             #
#----------------------------------------------------------#

if [ "$IPV4" == "yes" ] && [ "$IPV6" == "no" ] ; then
  tpl_suffix=""
elif [ "$IPV4" == "no" ] && [ "$IPV6" == "yes" ] ; then
  tpl_suffix="IPV6"
else
  tpl_suffix="IP46"
fi

# Parsing domain values
get_domain_values 'web'
tpl_file="$WEBTPL/$PROXY_SYSTEM/$PROXY$tpl_suffix.tpl"
old_tpl=$PROXY
conf="$HOMEDIR/$user/conf/web/$PROXY_SYSTEM.conf"
ip=''
ipv6=''
if [ "$IPV4" == "yes" ] ; then
  ip=$(get_real_ip $IP)
fi
if [ "$IPV6" == "yes" ] ; then
  ipv6=$(get_real_ipv6 $IP6)
fi

# Delete old vhost
del_web_config

# Add new vhost
PROXY="$template"
PROXY_EXT="$extentions"
tpl_file="$WEBTPL/$PROXY_SYSTEM/$template$tpl_suffix.tpl"
upd_web_domain_values
add_web_config
chown root:$user $conf
chmod 640 $conf

# Checking SSL
if [ "$SSL" = 'yes' ]; then
    tpl_file="$WEBTPL/$PROXY_SYSTEM/$old_tpl$tpl_suffix.stpl"
    conf="$HOMEDIR/$user/conf/web/s$PROXY_SYSTEM.conf"
    del_web_config

    tpl_file="$WEBTPL/$PROXY_SYSTEM/$template$tpl_suffix.stpl"
    add_web_config
    chown root:$user $conf
    chmod 640 $conf
fi

# Running template trigger
if [ -x $WEBTPL/$PROXY_SYSTEM/$template.sh ]; then
    $WEBTPL/$PROXY_SYSTEM/$template.sh $user $domain $ip $ipv6 $HOMEDIR $docroot
fi


#----------------------------------------------------------#
#                       Vesta                              #
#----------------------------------------------------------#

# Updating config
update_object_value 'web' 'DOMAIN' "$domain" '$PROXY' "$PROXY"
update_object_value 'web' 'DOMAIN' "$domain" '$PROXY_EXT' "$extentions"

# Restarting proxy
if [ "$restart" != 'no' ]; then
    $BIN/v-restart-proxy
    check_result $? "Proxy restart failed" >/dev/null
fi

# Logging
log_history "changed proxy template for $domain to $template"
log_event "$OK" "$EVENT"

exit
