#!/bin/bash
# info: delete system firewall
# options: NONE
#
# The script disables firewall support


#----------------------------------------------------------#
#                    Variable&Function                     #
#----------------------------------------------------------#

# Includes
source $devit/func/main.sh
source $devit/conf/devit.conf


#----------------------------------------------------------#
#                    Verifications                         #
#----------------------------------------------------------#

if [ -z "$FIREWALL_SYSTEM" ]; then
    exit
fi


#----------------------------------------------------------#
#                       Action                             #
#----------------------------------------------------------#

# Stopping firewall
$BIN/v-stop-firewall

# Updating FIREWALL_SYSTEM value
if [ -z "$(grep FIREWALL_SYSTEM $devit/conf/devit.conf)" ]; then
    echo "FIREWALL_SYSTEM=''" >> $devit/conf/devit.conf
else
    sed -i "s/FIREWALL_SYSTEM=.*/FIREWALL_SYSTEM=''/g" $devit/conf/devit.conf
fi


#----------------------------------------------------------#
#                       devit                              #
#----------------------------------------------------------#

# Logging
log_event "$OK" "$ARGUMENTS"

exit
