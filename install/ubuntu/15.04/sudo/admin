# Created by devit installer
Defaults env_keep="devit"
Defaults:admin !syslog
Defaults:admin !requiretty
Defaults:root !requiretty

# sudo is limited to devit scripts
admin   ALL=NOPASSWD:/usr/local/devit/bin/*
